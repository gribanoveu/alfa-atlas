// Образец итоговой структуры документа — не копировать буквально.
// Скелет папки создаётся тулом createDirectory (template: "restEndpoint"),
// см. references/tools.md.
//
// Пустых ячеек в таблицах от четырёх колонок не бывает: проверка K.4.2/K.5.2
// (check, kind: "standards") считает непройденной таблицу, где хотя бы одна
// ячейка пуста. Нечего написать — ставь дефис. Строки «Метод» и «Endpoint»
// занимают всю ширину через 5+| вместо четырёх пустых ячеек.
//
// Между заголовком документа и атрибутами НЕТ пустой строки: она закрывает
// шапку, и :toc: перестаёт действовать — оглавление не отрисуется.
= Rest-метод {имяРучки}
:sectnums:
:sectnumlevels: 3
:toc: left
:toclevels: 3
:toc-title: Оглавление

== Назначение
{Что делает ручка, в каком модуле живёт, для какой ветки процесса используется на текущий момент.}

// Блок ниже — только если метод является новой версией существующего
*Отличия от метода {имяСтаройРучки}*

- {отличие по транспорту}
- {отличие по расположению параметров}
- {отличие по составу вызовов и файлов}

== Описание входных/выходных параметров
=== Входные параметры

[cols="1,1,1,1,3,1"]
|===
| *Тип параметра* | *Параметр* | *Формат* | *Обязательность* | *Описание* | *Варианты значений*

|Метод 5+| POST
|Endpoint 5+| https://{host}/<сервис>/<путь>

| Header
| A-userId
| string (length 6 or 9)
| required
| Идентификатор УЛ интернет-банка. Заполняется входным параметром A-userId
| XAAAAA

| Header
| A-customerId
| string (length 6 or 9)
| required
| Идентификатор клиента. Заполняется входным параметром A-customerId
| UAAAAA

| Header
| A-userIp
| string
| optional
| IP УЛ.
| 64.233.165.113

| Header
| A-projectId
| string
| optional
| Идентификатор приложения (модуля приложения) потребителя.
| CORP-<ПРОЕКТ>

| Header
| A-clientType
| string
| required
| Тип сервиса инициатора запроса.
| FRONT

| Header
| A-channelId
| string
| required
| Идентификатор вызывающей системы (канала).
| NIB

| Path
| {pathParam}
| string
| required
| {описание}
| -

6+| Тело запроса

| Body
| {параметр}
| string
| required
| {чем заполняется — входной параметр, поле из БД или ответ предыдущего шага}
| -
|===

==== Пример запроса

include::request.adoc[]

=== Выходные параметры
[cols="1,1,3,1"]
|===
| *Параметр* | *Формат* | *Описание* | *Варианты значений*

| {поле}
| struct
| {что это}
| -

| {поле.вложенное}
| string
| {что это, откуда берётся значение}
| -
|===

==== Пример ответа

include::response.adoc[]

== Схема работы

[plantuml, "{имяРучки}", png]
----

include::{имяРучки}.puml[]

----

image:{имяРучки}.png[]

== Алгоритм работы
. Валидация входных параметров
. Шаг 1. {Название шага}
. Шаг 2. {Название шага} +
. Формирование ответа

=== Валидация входных параметров

[cols="1,2,3"]
|===
| *Параметр* | *Условие* | *Результат*

.2+|A-userId
|Параметр имеет значение null или пусто
|Вернуть исключение с http code 400 (Bad Request), указанием на некорректный параметр - "A-userId не указан", type = VALIDATION_ERROR и code = INCORRECT_CONTRACT
|Длина значения не равна 6 символам либо символы не являются алфавитно-числовыми
|Вернуть исключение с http code 400 (Bad Request), указанием на некорректный параметр - "A-userId должен содержать 6 алфавитно-цифровых символов", type = VALIDATION_ERROR и code = INCORRECT_CONTRACT

|{pathParam}
|{условие, при котором сущность считается не найденной}
|Вернуть ошибку 400, где type = VALIDATION_ERROR, code = INCORRECT_CONTRACT и описание "{текст}".

|===

=== Шаг 1. {Название шага}

{Одно-два предложения: зачем шаг и какой сервис вызывается.}

==== Входные параметры

|===
|*Тип параметра* |*Параметр* |*Формат* |*Обязательность* |*Описание* |*Варианты значений*

|Header
|A-projectId
|string
|optional
|Идентификатор приложения (модуля приложения) потребителя. Заполняется значением CORP-<ПРОЕКТ>
|CORP-<ПРОЕКТ>

|Body
|{параметр}
|string
|required
|{чем заполняется — входной параметр, поле из БД или ответ предыдущего шага}
|-

|===

include::../_external/{домен}/{сервис}/{метод}-request.adoc[]

==== Выходные параметры

Приведены только параметры, которые используются в методе.

|===
|*Параметр* |*Формат* |*Описание* |*Варианты значений*

|{поле}
|string
|{что это}
|-
|===

include::../_external/{домен}/{сервис}/{метод}-response.adoc[]

Если метод не вернул ошибку, перейти к следующему шагу, иначе остановить работу метода и вернуть ошибку (500 ошибка c type = SERVER_ERROR, code = {CODE}, описание: "{текст ошибки}").

=== Формирование ответа

Сформировать ответ следующим образом:

|===
|*Параметр*| *Правило заполнения*

|{поле ответа}
|{откуда берётся значение, с указанием шага}

|===

== Обработка ошибок
Описание приведено по ссылке ниже.

include::../CompositeException.adoc[]

*Коды ошибок*

[cols="2,2,1,1"]
|===
| *Условие* | *Описание* | *Type* | *Code*

|A-userId имеет значение null или пусто |A-userId не указан |VALIDATION_ERROR |INCORRECT_CONTRACT
|Шаг 1. {Название шага}. {Что пошло не так} |{Текст ошибки} |SERVER_ERROR |{CODE}
|===
