# Workspace Index Service (MVP)

## Цель

Разработать сервис `WorkspaceIndex`, отвечающий за индексацию рабочей области документации и предоставление данных для редактора.

Сервис является единым источником информации о документах проекта и используется для:

- диагностики ошибок;
- автодополнения;
- навигации по проекту.

На данном этапе не реализуются поиск использований, рефакторинг и кэширование индекса.

---

# Требования

## 1. Индексация рабочей области

После открытия проекта сервис должен выполнить обход файловой системы и построить индекс проекта.

Поддерживаемые типы файлов:

- `*.adoc`
- `*.md`
- `*.json`
- `*.yaml`
- `*.yml`

Архитектура должна позволять легко подключать новые типы документов.

---

## 2. Построение индекса

### Для каждого документа необходимо сохранить

- абсолютный путь;
- относительный путь;
- имя файла;
- тип документа;
- дату последнего изменения.

---

### Для AsciiDoc необходимо извлекать

#### Anchor

Пример

```adoc
[[installation]]

[#configuration]
```

Необходимо сохранить

- идентификатор;
- документ;
- строку;
- колонку.

---

#### Include

Пример

```adoc
include::common.adoc[]
```

Необходимо сохранить

- путь;
- документ-источник;
- строку;
- колонку.

---

#### XRef

Пример

```adoc
xref:install.adoc[]
xref:install.adoc#configuration[]
```

Необходимо сохранить

- документ назначения;
- anchor (если указан);
- документ-источник;
- строку;
- колонку.

---

#### Атрибуты

Пример

```adoc
:product-name: DocFlow
```

Необходимо сохранить

- имя;
- значение;
- документ;
- строку.

---

#### Изображения

Пример

```adoc
image::logo.svg[]

image::images/auth.png[]
```

Необходимо сохранить

- путь;
- документ;
- строку.

---

## 3. Инкрементальное обновление

После изменения файла сервис должен:

1. определить изменившийся файл;
2. удалить связанные записи из индекса;
3. повторно проиндексировать только данный файл;
4. обновить индекс.

Полная перестройка проекта выполняется только при открытии рабочей области.

---

## 4. Мониторинг файловой системы

Сервис должен отслеживать:

- создание файла;
- удаление файла;
- изменение файла;
- переименование файла.

После получения события индекс автоматически обновляется.

---

## 5. API сервиса

### Работа с документами

```kotlin
getDocument(path)

getDocuments()

findDocument(name)
```

---

### Работа с Anchor

```kotlin
findAnchor(id)

findAnchors(document)
```

---

### Работа с Include

```kotlin
findIncludes(document)
```

---

### Работа с XRef

```kotlin
findReferences(document)
```

---

### Работа с атрибутами

```kotlin
findAttribute(name)

getAttributes(document)
```

---

### Работа с изображениями

```kotlin
findImage(path)
```

---

## 6. Диагностика

Сервис должен выполнять базовую диагностику проекта.

### Ошибки

- отсутствует include-файл;
- отсутствует документ в xref;
- отсутствует anchor в xref;
- отсутствует изображение;
- дублирование anchor внутри проекта;
- циклический include.

Результат диагностики должен содержать:

- тип ошибки;
- сообщение;
- файл;
- строку;
- колонку;
- уровень (Error / Warning).

---

## 7. Автодополнение Monaco Editor

Сервис должен предоставлять данные для автодополнения.

### include

После ввода

```adoc
include::
```

редактор должен предложить список документов проекта.

---

### xref

После ввода

```adoc
xref:
```

редактор должен предложить документы проекта.

После выбора документа, содержащего anchor, редактор должен предлагать список anchor данного документа.

---

### image

После ввода

```adoc
image::
```

редактор должен предложить найденные изображения.

---

### Атрибуты

После ввода

```adoc
{
```

редактор должен предложить список известных атрибутов проекта.

---

## 8. Производительность

Требования

- индекс строится один раз при открытии проекта;
- изменение одного файла не должно приводить к полной перестройке;
- поиск данных осуществляется за O(1) или O(log n);
- хранение индекса выполняется в памяти;
- обновление индекса одного документа должно занимать менее 100 мс для файлов среднего размера.

---

## 9. Архитектура

```text
WorkspaceIndex
│
├── WorkspaceScanner
├── FileWatcher
├── ParserRegistry
│
├── DocumentRepository
├── AnchorRepository
├── IncludeRepository
├── ReferenceRepository
├── AttributeRepository
├── ImageRepository
│
├── DiagnosticsService
└── CompletionService
```

---

# Критерии приемки

Сервис считается реализованным, если:

- открытие проекта автоматически строит индекс;
- изменения файлов автоматически обновляют индекс;
- Monaco Editor получает автодополнение для `include`, `xref`, `image` и атрибутов;
- сервис обнаруживает битые ссылки, отсутствующие include, изображения и дубли anchor;
- индекс полностью хранится в памяти и обновляется без полной перестройки проекта.

## Зависимости

Для реализации индексации и диагностики необходимо использовать следующие библиотеки.

| Формат | Библиотека | Назначение |
|---------|------------|------------|
| AsciiDoc | `@asciidoctor/core` | Парсинг документа, получение AST, диагностика, извлечение include, xref, anchor, атрибутов |
| Markdown | `remark`, `remark-parse`, `remark-lint` | Парсинг Markdown и базовая диагностика |
| JSON | Monaco JSON Language Service | Валидация и диагностика JSON |
| YAML | `yaml` | Парсинг и диагностика YAML |
| XML *(опционально)* | `fast-xml-parser` | Проверка корректности XML |

Архитектура должна позволять заменять или добавлять парсер для нового типа документа без изменения остальных компонентов системы.

## Отображение состояния индексации

Во время построения или обновления индекса пользователь должен видеть текущее состояние процесса.

Статус отображается в нижней панели приложения (Status Bar) рядом с информацией о положении курсора (строка, столбец).

Возможные состояния:

- `$(sync~spin) Building workspace index...`
- `$(sync~spin) Updating index...`
- `$(check) Index ready`
- `$(warning) Index completed with warnings`
- `$(error) Index failed`

Дополнительно рекомендуется отображать прогресс:

```text
Building workspace index... (128 / 523)
```

или

```text
Updating index... (install.adoc)
```

После завершения индексирования отображается краткая статистика:

```text
Indexed 523 documents • 1348 anchors • 287 xref • 96 include • 2 warnings
```

Сервис `WorkspaceIndex` должен публиковать события изменения состояния, чтобы пользовательский интерфейс мог обновлять Status Bar без постоянного опроса.

## События WorkspaceIndex

Сервис должен публиковать события:

- IndexBuildingStarted
- IndexBuildingProgress
- IndexBuildingFinished
- IndexUpdated
- DiagnosticsUpdated

Интерфейс пользователя подписывается на данные события для обновления:

- Status Bar;
- списка ошибок;
- дерева проекта;
- автодополнения Monaco Editor.